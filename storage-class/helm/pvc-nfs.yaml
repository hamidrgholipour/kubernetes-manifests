apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs
  # namespace: storage
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-client   
  resources:
    requests:
      storage: 1Gi
